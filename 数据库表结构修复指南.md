# 🔧 数据库表结构修复指南

## 📊 问题分析

### 原始问题
1. 数据库表缺少 `user_type` 和 `points` 字段
2. User 实体类与数据库表结构不一致
3. ID 字段没有设置自增

---

## ✅ 已完成的修复

### 1. 更新 User 实体类
**文件：** `efh-user/src/main/java/com/efh/user/entity/User.java`

**修改内容：**
- ✅ 添加 `gender` 字段
- ✅ 移除 `@TableField(exist = false)` 注解
- ✅ 所有字段与数据库表完全对应

**最终字段列表：**
```java
- id              // BIGINT (继承自 BaseEntity)
- username        // VARCHAR(50)
- password        // VARCHAR(100)
- nickname        // VARCHAR(50)
- avatar          // VARCHAR(200)
- phone           // VARCHAR(20)
- email           // VARCHAR(100)
- gender          // TINYINT
- userType        // TINYINT
- status          // TINYINT
- points          // INT
- createTime      // DATETIME (继承自 BaseEntity)
- updateTime      // DATETIME (继承自 BaseEntity)
```

---

### 2. 更新 UserServiceImpl
**文件：** `efh-user/src/main/java/com/efh/user/service/impl/UserServiceImpl.java`

**修改内容：**
- ✅ 注册时设置所有默认值
- ✅ gender = 0 (未知)
- ✅ userType = 1 (普通用户)
- ✅ status = 1 (正常)
- ✅ points = 0 (初始积分)

---

### 3. 创建数据库更新脚本
**文件：** `docker/mysql/init/update_user_table.sql`

**脚本内容：**
```sql
-- 为两个数据库添加缺失字段
USE efh_user_0;
ALTER TABLE `user` ADD COLUMN `user_type` TINYINT DEFAULT 1;
ALTER TABLE `user` ADD COLUMN `points` INT DEFAULT 0;
ALTER TABLE `user` MODIFY COLUMN `id` BIGINT NOT NULL AUTO_INCREMENT;

USE efh_user_1;
ALTER TABLE `user` ADD COLUMN `user_type` TINYINT DEFAULT 1;
ALTER TABLE `user` ADD COLUMN `points` INT DEFAULT 0;
ALTER TABLE `user` MODIFY COLUMN `id` BIGINT NOT NULL AUTO_INCREMENT;
```

---

## 🚀 执行步骤

### 方式一：手动执行 SQL（推荐）

1. **连接到 MySQL**
   ```bash
   docker exec -it efh-mysql mysql -uroot -p123456
   ```

2. **执行更新脚本**
   ```sql
   USE efh_user_0;
   ALTER TABLE `user` ADD COLUMN `user_type` TINYINT DEFAULT 1 COMMENT '用户类型：1-普通用户，2-技师，3-商家' AFTER `gender`;
   ALTER TABLE `user` ADD COLUMN `points` INT DEFAULT 0 COMMENT '积分' AFTER `status`;
   ALTER TABLE `user` MODIFY COLUMN `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID';
   
   USE efh_user_1;
   ALTER TABLE `user` ADD COLUMN `user_type` TINYINT DEFAULT 1 COMMENT '用户类型：1-普通用户，2-技师，3-商家' AFTER `gender`;
   ALTER TABLE `user` ADD COLUMN `points` INT DEFAULT 0 COMMENT '积分' AFTER `status`;
   ALTER TABLE `user` MODIFY COLUMN `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID';
   ```

3. **验证表结构**
   ```sql
   USE efh_user_0;
   DESC user;
   ```

**预期结果：**
```
+-------------+--------------+------+-----+-------------------+
| Field       | Type         | Null | Key | Default           |
+-------------+--------------+------+-----+-------------------+
| id          | bigint       | NO   | PRI | NULL              |
| username    | varchar(50)  | NO   | UNI | NULL              |
| password    | varchar(100) | NO   |     | NULL              |
| nickname    | varchar(50)  | YES  |     | NULL              |
| avatar      | varchar(200) | YES  |     | NULL              |
| phone       | varchar(20)  | YES  | MUL | NULL              |
| email       | varchar(100) | YES  |     | NULL              |
| gender      | tinyint      | YES  |     | 0                 |
| user_type   | tinyint      | YES  |     | 1                 |
| status      | tinyint      | YES  |     | 1                 |
| points      | int          | YES  |     | 0                 |
| create_time | datetime     | YES  |     | CURRENT_TIMESTAMP |
| update_time | datetime     | YES  |     | CURRENT_TIMESTAMP |
+-------------+--------------+------+-----+-------------------+
```

---

### 方式二：使用脚本文件

```bash
# 复制脚本到容器
docker cp d:/LH/ev-forklift-hub/docker/mysql/init/update_user_table.sql efh-mysql:/tmp/

# 执行脚本
docker exec -it efh-mysql mysql -uroot -p123456 < /tmp/update_user_table.sql
```

---

## 🧪 测试验证

### 1. 重启用户服务
- 停止 UserApplication
- 重新启动 UserApplication

### 2. 测试注册接口
```bash
curl -X POST http://localhost:8081/api/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser001","password":"123456","nickname":"测试用户","phone":"13800138001"}'
```

**预期响应：**
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9..."
  }
}
```

### 3. 验证数据库
```sql
USE efh_user_0;
SELECT * FROM user WHERE username = 'testuser001';
```

**应该看到：**
- ✅ username: testuser001
- ✅ password: $2a$10$... (BCrypt 加密)
- ✅ nickname: 测试用户
- ✅ phone: 13800138001
- ✅ gender: 0
- ✅ user_type: 1
- ✅ status: 1
- ✅ points: 0
- ✅ create_time: 当前时间
- ✅ update_time: 当前时间

---

## 📋 完整的表结构定义

### 最终的 user 表结构

```sql
CREATE TABLE `user` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(100) NOT NULL COMMENT '密码（BCrypt加密）',
  `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
  `avatar` VARCHAR(200) DEFAULT NULL COMMENT '头像URL',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `gender` TINYINT DEFAULT 0 COMMENT '性别：0-未知，1-男，2-女',
  `user_type` TINYINT DEFAULT 1 COMMENT '用户类型：1-普通用户，2-技师，3-商家',
  `status` TINYINT DEFAULT 1 COMMENT '状态：0-禁用，1-正常',
  `points` INT DEFAULT 0 COMMENT '积分',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_phone` (`phone`),
  KEY `idx_create_time` (`create_time`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

---

## 🎯 字段说明

| 字段 | 类型 | 说明 | 默认值 | 索引 |
|------|------|------|--------|------|
| id | BIGINT | 用户ID | 自增 | 主键 |
| username | VARCHAR(50) | 用户名 | - | 唯一索引 |
| password | VARCHAR(100) | 密码（BCrypt） | - | - |
| nickname | VARCHAR(50) | 昵称 | NULL | - |
| avatar | VARCHAR(200) | 头像URL | NULL | - |
| phone | VARCHAR(20) | 手机号 | NULL | 唯一索引 |
| email | VARCHAR(100) | 邮箱 | NULL | - |
| gender | TINYINT | 性别 | 0 | - |
| user_type | TINYINT | 用户类型 | 1 | - |
| status | TINYINT | 状态 | 1 | 普通索引 |
| points | INT | 积分 | 0 | - |
| create_time | DATETIME | 创建时间 | 当前时间 | 普通索引 |
| update_time | DATETIME | 更新时间 | 当前时间 | - |

---

## 🔒 数据约束

### 唯一约束
- ✅ username（用户名唯一）
- ✅ phone（手机号唯一）

### 默认值
- ✅ gender = 0（未知）
- ✅ user_type = 1（普通用户）
- ✅ status = 1（正常）
- ✅ points = 0（初始积分）
- ✅ create_time = CURRENT_TIMESTAMP
- ✅ update_time = CURRENT_TIMESTAMP ON UPDATE

### 索引优化
- ✅ 主键索引：id
- ✅ 唯一索引：username, phone
- ✅ 普通索引：create_time, status

---

## ✅ 保证未来不会出现类似问题

### 1. 开发规范
- ✅ 实体类字段必须与数据库表字段完全对应
- ✅ 新增字段时同步更新实体类和数据库
- ✅ 使用 MyBatis-Plus 的字段映射注解

### 2. 数据库设计规范
- ✅ 所有表必须有主键
- ✅ 主键使用 AUTO_INCREMENT
- ✅ 所有字段必须有注释
- ✅ 合理使用默认值
- ✅ 重要字段添加索引

### 3. 代码规范
- ✅ 实体类使用 @TableName 指定表名
- ✅ 不存在的字段使用 @TableField(exist = false)
- ✅ 字段命名使用驼峰，数据库使用下划线

### 4. 测试规范
- ✅ 每次修改表结构后必须测试
- ✅ 验证所有 CRUD 操作
- ✅ 检查日志中的 SQL 语句

---

## 📝 检查清单

在启动服务前，请确认：

- [ ] 已执行数据库更新脚本
- [ ] 已验证表结构（DESC user）
- [ ] User 实体类已更新
- [ ] UserServiceImpl 已更新
- [ ] 已重新编译项目
- [ ] 已重启用户服务
- [ ] 已测试注册接口
- [ ] 已验证数据库中的数据

---

**现在请执行数据库更新脚本，然后重启服务测试！** 🚀
