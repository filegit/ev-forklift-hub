# 本地覆盖配置模板（可选，复制为 application-local.yml 后使用）
# 推荐优先使用项目根目录 .env.local + start-agent.bat，避免 YAML 里写密钥
efh:
  agent:
    llm:
      enabled: true
      api-key: ${LLM_API_KEY}
      base-url: ${LLM_BASE_URL:https://niceapi.ai/v1}
      model: ${LLM_MODEL:gpt-5.4}
